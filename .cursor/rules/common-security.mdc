---
description: Security checklist to apply before commits touching auth, secrets, or user input
alwaysApply: false
---

# Security

Before committing:
- No hardcoded secrets in source code — use env vars or a secret manager
- Validate and sanitize all user inputs
- Use parameterized queries for database access
- Ensure error messages do not leak internal details
