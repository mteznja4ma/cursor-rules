---
alwaysApply: true
---

# Coding Style

- Prefer immutable data: create new objects instead of mutating existing ones
- Organize code by feature/domain, not by type
- Keep files under 800 lines; extract when they grow beyond 400
- Keep functions under 50 lines
- Avoid nesting deeper than 4 levels
- No hardcoded values — use constants or config
